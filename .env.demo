# Demo Environment Variables for FastAPI Application
# Database Configuration (will be mocked for demo)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rental_ml_demo
DB_USERNAME=demo_user
DB_PASSWORD=demo_password
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# Redis Configuration (will be mocked for demo)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
REDIS_MAX_CONNECTIONS=10
REDIS_SOCKET_TIMEOUT=5
REDIS_SOCKET_CONNECT_TIMEOUT=5
REDIS_HEALTH_CHECK_INTERVAL=30

# Application Configuration
APP_ENV=demo
APP_DEBUG=true
APP_LOG_LEVEL=INFO

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=1
API_RELOAD=true

# Security
SECRET_KEY=demo_secret_key_not_for_production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ML Model Configuration
MODEL_CACHE_TTL=3600
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2